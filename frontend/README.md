# 프론트엔드 README

React Router v7 (framework mode, SSR) 기반 SRT 자동 예약 UI.

## 개발

```bash
npm install
npm run dev   # http://localhost:3000
```

## 환경변수

- `VITE_API_BASE` : 백엔드 API 베이스 URL (기본 `http://localhost:8000`)

## 빌드 / 실행

```bash
npm run build
npm run start
```
